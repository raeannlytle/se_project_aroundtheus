class Popup {
  constructor({ popupSelector }) {
    this._popupElement = document.querySelector(popupSelector);
  }

  open() {
    // opens popup
  }

  close() {
    // closes popup
  }

    _handleEscapeClose() {
      // listens for escape keydown
    }

    setEventListeners() {
      // sets event listeners
    }


}